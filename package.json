{
  "name": "blogs",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "tinacms dev --datalayer-port 9200 -c \"next dev\"",
    "dev:raw": "next dev",
    "kill:9000": "npx kill-port 9000 || true",
    "build": "tinacms build && next build",
    "start": "tinacms build && next start",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "lucide-react": "^0.540.0",
    "next": "14.2.5",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "tinacms": "^2.8.2"
  },
  "devDependencies": {
    "@tinacms/cli": "^1.10.2",
    "@types/node": "^20.0.0",
    "@types/react": "^18.2.0",
    "typescript": "^5.4.0"
  }
}
